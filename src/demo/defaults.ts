import { getDefaultPreset } from '@/demo/presets';

/** @deprecated Use getDefaultPreset().config */
export const DEFAULT_DEMO_CONFIG = getDefaultPreset().config;

/** @deprecated Use getDefaultPreset().customerPrompt */
export const DEFAULT_CUSTOMER_PROMPT = getDefaultPreset().customerPrompt;

export const DEMO_BUSINESS_ID = 'demo_biz_ephemeral';
export const DEMO_BUSINESS_SLUG = 'demo-ephemeral';

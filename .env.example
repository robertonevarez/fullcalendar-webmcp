# ScheduleMCP environment

# Absolute path to SQLite database file.
# Local default: ./data/schedulemcp.db
# Production (Render disk): /var/data/schedulemcp.db
DATABASE_PATH=

# Optional. When set, POST /api/demo/reset with
#   Authorization: Bearer <token>
# force-reseeds the demo database.
DEMO_RESET_TOKEN=
